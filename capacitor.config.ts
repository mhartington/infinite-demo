import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'v15-standalone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
